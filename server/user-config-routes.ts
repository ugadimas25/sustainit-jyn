import { Router } from 'express';

const router = Router();

// User Configuration Routes (RBAC System) - Disabled
// Authentication has been removed from the application
// These routes are no longer functional

export default router;
